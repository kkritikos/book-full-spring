This Terraform project enables to create a simulated Kubernetes cluster via minikube and to deploy in this cluster the 
backend of a book management application. It is assumed that minikube is already installed in the local system in which 
the Windows operating system is running.